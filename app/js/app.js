import * as flsFunctions from "./modules/functions.js";
flsFunctions.isWebp();

const featuredWorkDown = document.querySelector('.scrollDown').addEventListener('click', () => {
  console.log(`lll`)
});